# Data Contracts

Canonical public data lives in `content/public/`. Generated machine endpoints under `public/data/` are projections and must be byte-equivalent in meaning.

Stable identifiers must not be recycled. References between missions, quests, claims, contributions, reviews, and sources must resolve. Unknown references are release blockers. Schema changes require versioning and migration notes.
