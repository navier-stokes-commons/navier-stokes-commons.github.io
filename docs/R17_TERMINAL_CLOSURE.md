# R17 terminal closure

R17 beta.8 replaces the failed stacked R16 hero with the unified English-rich-site fluid authority.

Terminal corrections after executor adversarial integration:

- deployment SSOT audit validates the actual `nsc-forge-topology-v1` singular `mirror` object; the prior terminal bundle's plural `mirrors` expectation was erroneous;
- GitHub Pages and GitLab Pages are recorded as `configured-live-path-exercised` while stronger turnkey certification remains unclaimed;
- the motion audit distinguishes **field resumed** from **stale pointer wake**: after Resume, the field must visibly evolve while stored pointer-wake speed remains approximately zero until new pointer movement;
- generated root/English pages fail release if the literal `Play sweep` or legacy vortex debug controls remain;
- local deterministic audit/schema/integration failures are repairable engineering defects, not authorization blockers.

The only legitimate terminal blockers are external capabilities that cannot be repaired from the checkout (for example rejected remote authorization after credential preflight).
